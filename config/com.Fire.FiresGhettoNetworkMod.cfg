## Settings file was created by plugin FiresGhettoNetworkMod v1.4.23
## Plugin GUID: com.Fire.FiresGhettoNetworkMod

[01 - General]

## Controls verbosity in BepInEx log.
# Setting type: LogLevel
# Default value: Message
# Acceptable values: Warning, Message, Info
Log Level = Message

## When ON, appends the AILOD throttle's per-window stats (mobs examined,
## near/mid/far decision counts, nearest-peer distance range) to the
## consolidated [ServerStatus] line. Useful for tuning the near/far gates.
## Turn OFF once tuning is validated and you want a quieter log.
## Has no effect when Enable AI LOD Throttling is OFF — there's nothing to report.
# Setting type: Boolean
# Default value: true
Show AILOD in ServerStatus = true

## How often the consolidated [ServerStatus] line is logged on a dedicated server.
## Summarises MonoUpdaters tick rate, AI list sizes, BaseAI/MonsterAI tick counts,
## ZNetScene instantiation, our CreateDestroyObjects / IsActiveAreaLoaded gate
## behavior, and ServerOwnership transfer/release churn in a single line.
## Lower = more visibility, more log spam. Higher = quieter. Change takes effect on the next window.
# Setting type: Single
# Default value: 60
# Acceptable value range: From 10 to 3600
Diagnostic Rollup Interval (sec) = 60

## Temporary client diagnostic for the crash in cape cloth setup. Logs taking and leaving ship controls, shoulder
## item changes, every cloth collider handed to MagicaCloth2, every cloth built or destroyed, a per-frame check of
## each cape collider list, object creation bursts, data overwriting the local player, auto-tune probe steps, server
## clock corrections, every Steam setting FGN applies and a 5 second heartbeat with memory and compression totals.
## Each line is written before its step runs, so the last line before a crash names the step. Takes effect on the
## next start. Leave OFF for normal play.
## CLIENT-SIDE.
# Setting type: Boolean
# Default value: false
Enable Cape Crash Diagnostics = false

## Verbose, opt-in diagnostics for investigating items and tombstones sinking through
## structures. Off by default. When on, two probes run: a per-spawn probe that raycasts
## under each dropped item/tombstone and logs the ones at risk (and any that actually
## fall), and a one-shot startup audit that names build pieces left non-Solid (the load
## order that lets an item spawn before its support). The per-spawn probe adds real cost
## and log volume on a busy server, so leave this OFF for normal play and the live read —
## turn it on only to investigate a suspected fall-through. These probes only observe and
## log; they do not change any physics.
# Setting type: Boolean
# Default value: false
Enable Fall-Through Diagnostics = false

[02 - Client Performance]

## How aggressively the client consumes incoming zone-stream backlog per frame.
## 1 = vanilla (one CreateObjects pass per frame, capped by Valheim).
## 2 = double the per-frame cap (faster zone load, bigger frame hitches).
## 4 = quadruple (zone-cross stutter masking on capable machines).
## Auto-Tune may override this on the client based on measured frame time.
## CLIENT-ONLY — no effect on server.
# Setting type: Int32
# Default value: 1
# Acceptable value range: From 1 to 8
Zone Load Batch Size = 1

## Steam recv-buffer size for inbound network packages. Bigger = fewer dropped packets
## if the client briefly stalls (GC pause, disk hitch), but uses more RAM.
## Auto-Tune may override this on the client based on measured tier.
# Setting type: Int32
# Default value: 262144
# Acceptable value range: From 65536 to 4194304
ZPackage Receive Buffer Bytes = 262144

## Replace vanilla's fixed 10/100 per-frame ZDO instantiation cap with a per-frame ms
## budget that drains incoming objects across multiple ticks. Eliminates the big spike
## when crossing into a heavy zone. Disable to fall back to the cap-bump transpiler
## (set 'Zone Load Batch Size' to control its multiplier).
## OFF by default: instantiating this fast can spawn a creature/item the instant its ZDO
## arrives — before the structure it rests on, when that ZDO lags a tick behind — which can
## let tames slip locked pens or drop items through floors on zone load. Opt in for the
## smoother zone crossings if your world doesn't hit that.
## CLIENT-ONLY — no effect on dedicated server.
# Setting type: Boolean
# Default value: false
Enable Time-Slice Instantiation = false

## Per-frame millisecond budget for ZDO → GameObject instantiation when time-slicing
## is enabled. Lower = smoother frame times during zone load (longer ramp-in); higher
## = zone loads finish faster (bigger spikes). 3 ms keeps 60-fps clients comfortable.
## Auto-Tune overrides this with tier-specific values when active.
# Setting type: Int32
# Default value: 3
# Acceptable value range: From 1 to 16
Instantiation Budget Ms = 3

## Hard ceiling on instantiations per frame regardless of how much budget remains.
## Belt-and-suspenders against a runaway pending list.
## Auto-Tune overrides this with tier-specific values when active.
# Setting type: Int32
# Default value: 100
# Acceptable value range: From 10 to 500
Max Instances Per Frame = 100

## When the pending instantiation list grows past 'Safety Fallback Threshold' items
## (teleport into megabase, freshly-loaded zones), widen the per-frame budget so we
## don't bleed across many seconds. Capped at 16 ms so we never burn an entire frame.
# Setting type: Boolean
# Default value: true
Safety Fallback Enabled = true

## Pending-ZDO count at which the safety fallback widens the instantiation budget.
# Setting type: Int32
# Default value: 5000
# Acceptable value range: From 100 to 50000
Safety Fallback Threshold = 5000

## Maximum ZNetScene instances the client destroys per CreateDestroyObjects pass.
## Vanilla destroys every out-of-area instance in a single frame, producing a
## multi-second hitch when leaving a heavily-loaded zone (e.g. ~150k instances
## on a megabase). The throttle defers the surplus to subsequent frames so the
## departure cost spreads out smoothly.
## 0 = no throttle (vanilla single-frame destruction).
## 200 = ~12s to clear a 150k-instance backlog at 60 fps, with each frame's
## destroy cost roughly equal to instantiating 200 objects.
## CLIENT-ONLY — no effect on dedicated server.
# Setting type: Int32
# Default value: 200
# Acceptable value range: From 0 to 5000
Max Destroys Per Frame = 200

[03 - Player Sync]

## Boosts server send priority for player ZDOs so they sync before terrain/object ZDOs.
## Reduces the floaty delayed movement you see on other players.
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable High-Frequency Position Updates = true

## How aggressively player ZDOs are prioritized over other ZDOs (1.0 = vanilla, 2.5 = recommended).
## Higher values push player positions to the front of the send queue more strongly.
# Setting type: Single
# Default value: 2.5
# Acceptable value range: From 1 to 5
Position Update Multiplier = 2.5

## Smooths other players' movement on your client by interpolating between received network positions.
## Eliminates the snapping/teleporting caused by discrete 50ms network updates.
## Disabled by default — the rest of the mod's networking improvements (higher update rate, server
## ZDO priority boost, larger Steam buffers) usually deliver positions smoothly enough on their own,
## and interpolation adds a small render-lag that some players prefer to avoid. Turn ON if you still
## see other players snap/teleport even on a healthy connection.
## CLIENT-ONLY — no server impact.
# Setting type: Boolean
# Default value: false
Enable Client-Side Interpolation = false

## Extrapolates other players' positions forward between network updates using their last known velocity.
## Can help on high latency (>100ms) but may cause overshooting at low ping.
## Disabled by default — only enable if interpolation alone feels laggy.
## CLIENT-ONLY — no server impact.
# Setting type: Boolean
# Default value: false
Enable Client-Side Prediction = false

## When a remote player's packets arrive faster than this interval (seconds),
## smoothing is disabled entirely and vanilla movement renders directly.
## Default 0 = always smooth (recommended). The earlier default of 0.05
## (vanilla 20Hz) collapsed the smoothing-strength formula to ~0 under
## healthy traffic, which made BOTH this slider and Smoothing Max feel
## like no-ops and produced visible snaps every time inter-packet jitter
## crossed the threshold. Raise above 0 only if you specifically want
## fast packets to bypass smoothing (LAN / very low-latency servers).
# Setting type: Single
# Default value: 0
# Acceptable value range: From 0 to 0.2
Smoothing Min Interval (s) = 0

## When a remote player's packets arrive slower than this interval (seconds),
## smoothing runs at full strength. Between Min and Max the strength fades
## in linearly so the handoff is invisible. Raise it for a more aggressive
## fade-in (less smoothing on borderline-laggy connections); lower it for a
## snappier ramp to full smoothing.
# Setting type: Single
# Default value: 0.2
# Acceptable value range: From 0.05 to 0.5
Smoothing Max Interval (s) = 0.2

[04 - Networking]

## Enable Deflate network compression (highly recommended). It is negotiated per player, so it only
## engages with players whose FGN uses the same compression format. Data Valheim already compresses
## (terrain edits, tar pits, map tables) is sent as is rather than compressed again.
# Setting type: Boolean
# Default value: true
Enable Compression = true

## How often the server SENDS ZDO updates to clients. This is a NETWORK send-cadence
## setting ONLY — it does NOT change the world/game tick, day length, smelter/cook timers,
## cooldowns, or any simulation speed. Higher = other players/creatures look smoother to
## you, at the cost of more bandwidth.
## 100% (20 sends/sec) matches vanilla and is the safe default. 150% (30 sends/sec) can look
## smoother on high-pop servers with bandwidth headroom; lower it if bandwidth is tight.
## SERVER-ONLY.
# Setting type: UpdateRateOptions
# Default value: _100
# Acceptable values: _150, _100, _75, _50
ZDO Send Rate = _100

## Send queue size. Higher helps high-player servers.
# Setting type: QueueSizeOptions
# Default value: _32KB
# Acceptable values: _80KB, _64KB, _48KB, _32KB, _vanilla
Queue Size = _32KB

## When ON, reflectively patches the 20 KB GetSendQueueSize gate inside every loaded ServerSync.ConfigSync (AzuEPI, Marketplace, EpicLoot, Wizardry, EW Data, etc.) AND ServerCharacters.Shared copy so their fragment loops keep up with our raised ZDOMan cap.
## Replaces the old BulkTransferGuard which suppressed ZDOMan.SendZDOs during bulk bursts and incidentally starved player position ZDOs (the source of 'players flying / teleporting / hits from across the map' complaints).
## Disable as a kill switch if you suspect the scan is causing freezes or false-positive patching.
## Both sides — applies on client and dedicated server.
# Setting type: Boolean
# Default value: true
Enable Bulk Transfer Queue Boost = true

## Caps how much of the Steam per-connection send buffer the raised ServerSync gates may collectively claim, so many ServerSync mods (Azu/EpicLoot/Marketplace/EW/etc.) can't stack their raised gates and overflow the buffer (the cause of the heavy-area peer disconnects). The per-mod gate is computed as min(Queue Size, (buffer * this% / ServerSync-mod-count)), floored at the vanilla 20 KB so it never throttles tighter than stock. The buffer is 512 KB by default, or the larger value set when FiresSteamworksPatcher is installed. 40% leaves headroom for ZDO + RPC traffic. Lower if you still see heavy-area disconnects; raise if config syncs feel slow on join. Only matters when Bulk Transfer Queue Boost is ON.
# Setting type: Int32
# Default value: 40
# Acceptable value range: From 10 to 80
Bulk Transfer Budget Percent = 40

[05 - Networking - Steamworks]

## Minimum send rate Steam will attempt. Steam's adapter has a sticky-down quirk — peers that back off toward this value tend to stay there. Keep it well above unplayable.
# Setting type: SendRateMinOptions
# Default value: _512KB
# Acceptable values: _1024KB, _768KB, _512KB, _256KB, _150KB
Send Rate Min = _512KB

## Maximum send rate Steam will attempt. This is 'permission to burst' — Steam still ramps adaptively between Min and Max, this just removes the artificial ceiling.
# Setting type: SendRateMaxOptions
# Default value: _2048KB
# Acceptable values: _8192KB, _4096KB, _2048KB, _1024KB, _768KB, _512KB, _256KB, _150KB
Send Rate Max = _2048KB

## MAX-THROUGHPUT MODE. Overrides Auto-Tune AND the Send Rate Min/Max above, lifting Steam's
## send rate, send buffer, and recv buffer / per-message ceiling to their proven unlocked
## maximums — the same lifts fgn_socketramp uses to reach ~40 MB/s, versus the ~8 MB/s the
## High tier caps everyday traffic at. Applies LIVE the instant you toggle it (no reconnect).
## For a server->client transfer, set it on BOTH sides: the server lifts its outbound, the
## client lifts its inbound. The recv side needs FiresSteamworksPatcher installed.
## This trades Steam's conservative congestion ceiling for raw headroom and lets the buffers
## grow large under load, so leave it OFF for normal play and ON for high-bandwidth links or
## benchmarking.
# Setting type: Boolean
# Default value: false
HYPERBOOST = false

[06 - Auto-Tune]

## Run a brief network/hardware probe on first login to a server and pick a perf tier (LOW/MED/HIGH).
## Effective values shadow the BepInEx config — your manual settings are never overwritten.
## Disable to use only the values set above. CLIENT-SIDE only.
# Setting type: Boolean
# Default value: true
Enable Client Auto-Tune = true

## When the mod runs on a dedicated server, score the host's CPU/RAM and APPLY a tier preset to
## ZDO throttle / AI LOD / RPC AoI / queue size / extended zone radius / update rate / Steam buffers.
## Default ON — the tier preset is conservative and the asymmetric send-rate pattern keeps slow
## clients safe (Min stays at the LOW baseline regardless of tier). Disable if you've manually tuned
## every server knob and want your values used verbatim. SERVER-SIDE only.
# Setting type: Boolean
# Default value: true
Enable Server Auto-Tune = true

## When on, the server logs its own self-tune tier + recommended values at startup, and periodically
## logs the median tier reported by connected clients with suggested adjustments. Pure logging —
## no settings are changed. SERVER-SIDE only.
# Setting type: Boolean
# Default value: true
Log Server Suggestions = true

## When on, ignore the cached tier on disk and re-probe every time you join. Costs ~50KB of probe
## traffic per join but reflects current ISP/route conditions. CLIENT-SIDE only.
# Setting type: Boolean
# Default value: false
Retune On Every Login = false

## Your machine's measured tier is the CEILING — a great connection can never push you above
## what your hardware can actually handle. Only a genuinely BAD connection pulls your tier DOWN,
## and this caps how far. 1 (default) = a bad link drops you one tier (e.g. a strong PC on a
## 170ms link lands MED, not LOW). 0 = connection never lowers your tier (machine only). 2 = a
## bad link can drop you two tiers. Okay/medium ping costs nothing either way. CLIENT-SIDE only.
# Setting type: Int32
# Default value: 1
# Acceptable value range: From 0 to 2
Link Downgrade Cap = 1

## Server-side closed-loop send-rate controller. Each peer starts at the Auto-Tune/manual send rate and ramps UP toward the HYPERBOOST ceiling while it's delivering near that rate with a clear send-queue, easing back toward the real measured throughput (down to the tier's SendRateMin) only when the queue stays congested AND delivery can't keep up for several ticks. Pins SendRateMin = SendRateMax per connection so Steam's own sticky-down adapter can't park the peer near the floor — each client converges to its own real link capacity. Defers to HYPERBOOST while that's on. SERVER-SIDE only.
# Setting type: Boolean
# Default value: true
Adaptive Send Rate = true

[07 - Auto-Tune - Probe]

## Wait this long AFTER the player has actually arrived in the world (Game.m_playerInitialSpawn
## fired) before starting the probe. Lets the post-spawn burst — inventory equip, ZDO
## zone-load for the spawn point, post-spawn mod work — subside so we don't sample latency
## while Valheim's own arrival traffic is queued ahead of our pings. 30s is the safe default;
## a server with heavy mod-driven sync (large worlds, many players) may benefit from 45-60.
## Worst-case modpacks on slow servers may need 75-90s to fully settle. Lower only if you
## know your fast-load mod handles arrival-burst traffic well.
# Setting type: Single
# Default value: 30
# Acceptable value range: From 2 to 90
Start Delay Seconds = 30

## Hard cap on how long to wait for the local player to actually finish spawning into the
## world before forcing the probe to start anyway. The probe gates on Game.m_playerInitialSpawn
## (the same event that fires the '$text_player_arrived' chat message) so the probe doesn't
## start while the player is still mid-load — that event normally fires within tens of
## seconds of connect, but heavy modpacks with large worlds + slow disks can take 90s+.
## This timeout is the safety valve: if the spawn fails entirely we eventually proceed
## anyway rather than leave the client stuck at a degraded default tier forever. 180s
## covers worst-case modpack loads on slow disks; raise only if you have repro evidence
## the timeout is firing on a successful spawn.
# Setting type: Single
# Default value: 180
# Acceptable value range: From 30 to 600
Player Arrival Timeout Seconds = 180

## How long to wait for a single ping echo before giving up. On timeout we default to LOW tier.
# Setting type: Single
# Default value: 5
# Acceptable value range: From 1 to 15
Ping Timeout Seconds = 5

## Number of pings to send for the latency probe. The probe also fires one untracked
## warmup ping first (clears Steam TCP slow-start), and drops the single worst sample
## before computing stats so transient post-spawn queue contention can't tank the
## result on a healthy link. 10 is the sweet spot — enough samples for outlier
## trimming + IQR jitter to be stable; not so many that probe traffic becomes notable.
# Setting type: Int32
# Default value: 10
# Acceptable value range: From 5 to 20
Ping Count = 10

## If any single ping round-trip exceeds this, we hard-default to LOW tier and skip the
## bandwidth probe entirely — the client is already struggling, no point making them prove
## it twice.
# Setting type: Int32
# Default value: 2000
# Acceptable value range: From 500 to 5000
Ping Abort Ms = 2000

## Size of the bandwidth-test payload requested from server. Only runs if the latency probe
## puts the client at MED or HIGH tier — LOW-tier clients skip this entirely.
## 
## Larger payloads measure throughput more honestly: a request-response with a small
## payload is dominated by RTT, not actual link capacity. With a 64ms RTT, a 32KB payload
## ceilings at ~500 KB/s no matter what the link can deliver. 128KB at the same RTT can
## report up to ~2 MB/s — the transfer time finally dominates the RTT. Default raised
## from 32KB → 128KB on 2026-05-11 because the small-payload probe was systematically
## under-reporting on healthy links and incorrectly tier-downgrading.
## Tradeoff: probe takes ~1.5s longer per session and consumes ~256KB of extra one-time
## bandwidth on the dedicated server.
# Setting type: Int32
# Default value: 131072
# Acceptable value range: From 8192 to 524288
Bandwidth Probe Bytes = 131072

[08 - Auto-Tune - Monitor]

## After the initial probe, keep periodically re-probing latency over the session and
## refine the tier from a rolling average. Catches transient server overload (settling
## to a higher tier once the initial-sync flood ends) and ISP/route weather changes.
## Each re-probe is latency-only — no extra bandwidth probe — so cost is ~5KB per cycle.
## CLIENT-SIDE only.
# Setting type: Boolean
# Default value: true
Enable Rolling Monitor = true

## How often to re-probe latency after the initial probe. Smaller values catch transient
## issues faster but cost more probe traffic; larger values are quieter but slower to react.
# Setting type: Single
# Default value: 5
# Acceptable value range: From 1 to 30
Re-Probe Interval Minutes = 5

## Number of recent re-probe results held in the rolling buffer for averaging. Tier is
## the most-common tier in the buffer; promotion requires 2 consecutive observations,
## demotion requires 3 — biased to keep current tier rather than oscillate.
# Setting type: Int32
# Default value: 5
# Acceptable value range: From 3 to 10
Rolling Window Size = 5

[09 - Dedicated Server]

## Requires restart. Selects the networking backend for a DEDICATED SERVER.
## vanilla = respect the command-line -crossplay flag (DEFAULT — does NOT change how your server connects).
## steamworks = force Steam-only; DISABLES crossplay. Best performance for an all-Steam playerbase, but Xbox / Game Pass / PlayStation players cannot join.
## playfab = force crossplay ENABLED (PlayFab matchmaking) regardless of the -crossplay flag.
# Setting type: ForceCrossplayOptions
# Default value: vanilla
# Acceptable values: vanilla, playfab, steamworks
Force Crossplay = vanilla

## Max players on dedicated server. Requires restart.
# Setting type: Int32
# Default value: 10
# Acceptable value range: From 1 to 999
Player Limit = 10

## Max players advertised to matchmaking (Steam server browser → BattleMetrics, PlayFab session/Party). Independent of the actual in-game limit set by 'Player Limit' — useful when an operator wants their server listed as '/500' for marketing while running a real 30-slot cap. 0 = mirror 'Player Limit' (advertised matches reality). Requires restart.
# Setting type: Int32
# Default value: 0
# Acceptable value range: From 0 to 9999
Advertised Player Limit = 0

[10 - Diagnostics]

## Server-only, opt-in. When ON, samples each peer's outbound send-queue every 5s and logs the peak each minute, so you can see how close live traffic gets to the cliff fgn_socketramp finds. OFF (default) = the sampler never runs = zero performance cost. fgn_headroom prints a snapshot on demand.
# Setting type: Boolean
# Default value: false
Send Queue Headroom Monitor = false

## Verbose per-tick diagnostics for the Adaptive Send Rate controller — each managed peer's cap, delivered throughput, queue depth and the branch taken, every tick. Useful for tuning, noisy for normal play. OFF by default. SERVER-SIDE only.
# Setting type: Boolean
# Default value: false
Log Adaptive Send Rate Ticks = false

[10 - Server Authority]

## Server-side RPC filtering — drops unnecessary DamageText/HealthChanged RPCs before routing
## and prevents SetTarget exploits (targeting players/tamed creatures).
## Saves bandwidth and hardens server security.
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable RPC Router = true

## When enabled, broadcast RPCs targeting a specific ZDO are only forwarded to peers
## within the configured radius of that ZDO. Massively reduces bandwidth on busy servers.
## RPCs without a target ZDO (global RPCs) are always broadcast to all peers.
## Requires Enable RPC Router = true. SERVER-ONLY.
# Setting type: Boolean
# Default value: true
Enable RPC Area-of-Interest = true

## Distance (meters) from the target ZDO within which peers will receive the RPC.
## Vanilla active area is ~7 zones × 64m = ~448m. Default 256m covers nearby players.
## Set higher if players report missing interactions at distance.
# Setting type: Single
# Default value: 256
# Acceptable value range: From 64 to 1024
RPC AoI Radius = 256

## Comma-separated Unity layer names the dedi raycasts against when rescuing a mob
## that fell below the kill plane (y < -5000). Default mirrors vanilla BaseAI's
## solid-ray mask so any vanilla collider catches the mob. If your modlist ships
## custom terrain on a non-vanilla layer (RPGmaker overlay, etc.) and you see mobs
## permanently parked instead of recovering, add that layer name here. The cache
## refreshes when this value changes; no restart needed.
# Setting type: String
# Default value: Default,static_solid,Default_small,piece,terrain,vehicle
Dedi Fell-Out Rescue Layers = Default,static_solid,Default_small,piece,terrain,vehicle

## Makes the server fully authoritative over zones, ZDO ownership, monster AI, events, etc. (does NOT override your existing ship fixes).
## 
## The RPC router and RPC area-of-interest, ZDO delta compression, ZDO throttling, AI LOD and the
## WearNTear server optimization do not need this: each follows its own toggle either way.
## 
## Disabled by default — enable manually on your DEDICATED SERVER if desired.
## 
## WARNING: THIS IS A SERVER-ONLY FEATURE!
## Enabling this on a CLIENT will cause INFINITE LOADING SCREEN.
## The mod automatically disables it on clients regardless of this setting.
# Setting type: Boolean
# Default value: false
Enable Server-Side Simulation = false

## EXPERIMENTAL — defaults OFF. Direct port of the original Serverside Simulations mod's
## ZDOMan.ReleaseNearbyZDOS prefix. When enabled, the server takes ownership of EVERY
## persistent ZDO in any peer's active area (mobs, ships, terrain, structures, items, doors,
## the whole world). Peers no longer own anything.
## 
## WHEN TO ENABLE: you've validated the rest of your modlist plays nicely with broad
## server ownership and you want maximum server-side simulation (offloads client CPU,
## flips the bandwidth pattern so server send dominates).
## 
## WHEN TO LEAVE OFF: you're running a heavy modpack, you've seen mob freezes or
## interaction issues after a previous attempt, or you don't know yet. Leaving this off
## preserves vanilla peer ownership — every other server-authority feature (zones,
## spawning, raids, throttling) still works without it.
## 
## REQUIRES: ConfigEnableServerAuthority = true AND running on a dedicated server.
## TOGGLE IS INDEPENDENT — flip this without touching ConfigEnableServerAuthority.
# Setting type: Boolean
# Default value: false
Enable Server ZDO Ownership Transfer (EXPERIMENTAL) = false

## EXPERIMENTAL — defaults OFF. Selective variant of the broad ownership transfer above.
## Only Character (non-Player) and Ship prefabs are claimed by the server. Drops,
## containers, doors, signs, workstations, pickables, beds, traders, wards, voxel
## terrain, built structures, and carts all stay under vanilla peer ownership.
## 
## RATIONALE: broad SSS-style ownership (the toggle above) exposes interaction-RPC
## race conditions — `removedrops` failing for mob-dropped items, voxel mining/flattening
## breaking intermittently under load, carts shaking/sinking when parked. Selective scope
## avoids all three by keeping interactables on the vanilla peer-owned path.
## 
## MUTUALLY EXCLUSIVE with the broad toggle above. If both are true, this selective
## variant takes precedence (the safer choice — drops/voxel/etc. stay working).
## 
## REQUIRES: ConfigEnableServerAuthority = true AND running on a dedicated server.
# Setting type: Boolean
# Default value: false
Enable Server ZDO Ownership Transfer — Selective (EXPERIMENTAL) = false

## Additional zone layers the server pre-loads around players for smoother zone transitions.
## 0 = vanilla (no extra pre-load)
## 1 = +1 layer (recommended, ~7x7 zones total)
## 2 = +2 layers (~9x9 zones)
## 3 = +3 layers (~11x11 zones)
## 
## Higher values reduce stutter when crossing zone borders but increase server CPU/RAM usage.
## SERVER-ONLY — clients ignore this setting.
# Setting type: Int32
# Default value: 1
# Acceptable value range: From 0 to 3
Extended Zone Radius = 1

## Bias each peer's active-area center forward along their velocity vector so the
## server starts loading zones BEFORE the peer crosses the boundary. Composes with
## 'Extended Zone Radius' — the symmetric ring still expands, the center just slides
## forward. By the time the peer arrives, the predicted zones are already loaded.
## SERVER-ONLY — clients ignore this setting.
# Setting type: Boolean
# Default value: true
Enable Predictive Zone Streaming = true

## How many seconds ahead the server projects each peer's position when deciding
## which zones to pre-load. 3 s gives one zone of headroom at running speed.
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0.5 to 10
Prediction Lookahead Sec = 3

## Minimum smoothed speed (m/s) below which prediction is suppressed and the peer's
## real position is used. Stops idle / slow-walking peers from triggering pre-load.
# Setting type: Single
# Default value: 2
# Acceptable value range: From 0.5 to 20
Prediction Min Velocity = 2

## Hard cap on prediction distance, expressed in zones (64 m each). Stops a
## teleporting / glitching peer from subscribing to zones across the world.
# Setting type: Int32
# Default value: 9
# Acceptable value range: From 1 to 25
Prediction Max Lookahead Zones = 9

## Reduce update frequency for distant ZDOs (creatures/structures far away) to save bandwidth.
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable ZDO Throttling = true

## Distance (meters) beyond which ZDOs are throttled (lower update rate).
## 0 = disable throttling.
## Recommended: 400-600m.
# Setting type: Single
# Default value: 500
# Acceptable value range: From 0 to 1000
ZDO Throttle Distance = 500

## Reduce FixedUpdate frequency for distant AI (saves server CPU).
## Nearby AI stays full speed for smooth combat.
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable AI LOD Throttling = true

## Full-speed AI within this range (meters).
# Setting type: Single
# Default value: 100
# Acceptable value range: From 50 to 200
AI LOD Near Distance = 100

## Beyond this distance, AI is throttled (meters).
# Setting type: Single
# Default value: 300
# Acceptable value range: From 200 to 600
AI LOD Far Distance = 300

## Update multiplier for throttled AI (0.5 = half speed, 0.25 = quarter). Lower = more savings.
# Setting type: Single
# Default value: 0.5
# Acceptable value range: From 0.25 to 0.75
AI LOD Throttle Factor = 0.5

## Only engage FGN's send-side optimizations (distant-ZDO throttling, player
## boost, AI LOD) when a peer's send queue is actually backing up. On a server
## with bandwidth to spare, FGN leaves vanilla update order untouched — leaner
## and lower-latency. Turn OFF to force the optimizations on at all times.
## SERVER-ONLY.
# Setting type: Boolean
# Default value: true
Adaptive Throttling = true

## How full a peer's send queue must get (percent of cap) before Adaptive
## Throttling engages the optimizations. Lower = engages sooner.
## SERVER-ONLY.
# Setting type: Int32
# Default value: 50
# Acceptable value range: From 10 to 100
Congestion Threshold = 50

[11 - Ship Fixes]

## Apply permanent autopilot + jitter fixes to ALL ships.
# Setting type: Boolean
# Default value: true
Enable Universal Ship Fixes = true

## Server authoritatively simulates ship physics.
## Disabled by default — enable manually if you want the server to drive ship physics.
## 
## This also gates whether Selective (V3) ZDO ownership claims ships. Ownership IS simulation:
## whoever owns a hull runs its Rigidbody, and vanilla only applies impact damage on the owner,
## so a server that owns an empty boat can collide it against its own streaming colliders and
## damage it on flat water. Left OFF, ships stay peer-owned and the sailing client simulates
## them exactly as in vanilla, while creatures still move to the server.
## NOTE: the BROAD (V2) ownership toggle ignores this and claims ships regardless.
# Setting type: Boolean
# Default value: false
Server-Side Ship Simulation = false

[12 - Advanced]

## Vanilla decides whether something has fallen out of the world by comparing it to the
## HEIGHTMAP, which cannot see build pieces. Digging moves the heightmap, so pits and mines
## are fine, but anything resting on a piece BELOW the heightmap is not: a tame penned on a
## cellar floor under a mound, or a gravestone in that cellar, reads as under the world and
## is teleported up to the dirt, through the floor that was holding it. With this on, that
## one check asks what is actually underneath first (see 'Dedi Fell-Out Rescue Layers' for
## the surfaces that count) and only falls back to the heightmap when nothing is there.
## Affects Character.UnderWorldCheck and TombStone.PositionCheck. Costs a single raycast,
## and only in the moment vanilla was about to teleport something.
# Setting type: Boolean
# Default value: true
Fix Ground Snap Through Floors = true

## Diagnostic: log each peer's send-queue health every 10s on a dedicated
## server. Useful when investigating lag, but writes ~1 line per player per
## 10s to the log. Leave OFF for normal play. SERVER-ONLY.
# Setting type: Boolean
# Default value: false
Log Send Queue Heartbeat = false

## OFF by default. When ON, logs every Harmony patch attached to the AI tick,
## instantiation, and zone-gate methods this mod cares about — useful when
## troubleshooting mod-conflict scenarios (another mod's transpiler stomping our
## prefix, etc.) or when bringing up a new feature.
## 
## The runtime [ServerStatus] rollup is the ongoing health indicator; this toggle
## is only useful when you suspect Harmony itself didn't attach something.
# Setting type: Boolean
# Default value: false
Enable Boot Patch Verification = false

## OFF by default. When ON, adds enriched [BigZdoDiag] log lines next to vanilla's
## existing 'Writing a lot of data; X items, is not optimal' warning so you can see
## which ZDO is bloating its extra-data buckets (uid, prefab name, world position,
## owner peer id, bucket type and count).
## 
## NOTE: the underlying warning is emitted by vanilla Valheim, not by this mod.
## This toggle only controls whether we ATTACH context to it. Turn this on when you
## see the vanilla warning and want to track down the offending entity; leave it off
## otherwise so the log stays quiet.
# Setting type: Boolean
# Default value: false
Enable Large ZDO Diagnostics = false

## On re-syncs, only send ZDO fields that changed since last send to each peer.
## Initial sync always sends full ZDO state. Re-syncs only send the diff.
## Significant bandwidth reduction for high-field ZDOs (creatures, players) where
## only 1-2 fields change per tick (e.g. health, position).
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable ZDO Delta Compression = true

## Skips the wear and support update for building pieces whose damage modifiers are all
## Immune or Ignore (Infinity Hammer, admin-flagged pieces), on the server that owns them.
## Every other piece updates exactly as in vanilla.
## SERVER-ONLY — no effect on client.
# Setting type: Boolean
# Default value: true
Enable WearNTear Server Optimization = true

## CLIENT-side counterpart to the WearNTear server optimization. Short-circuits the
## expensive WearNTear.UpdateSupport call (Physics.OverlapBoxNonAlloc per piece) for
## pieces whose damage modifiers are all Immune/Ignore — e.g. Infinity Hammer pieces.
## Pins m_support at the material's max value so neighbouring mortal pieces still
## see full support when querying. Massive steady-state CPU saving in megabases
## dominated by invulnerable pieces.
# Setting type: Boolean
# Default value: true
Enable Invulnerable Support Skip = true

## SERVER-ONLY defensive cleanup. Before vanilla ZNetScene.RemoveObjects walks
## m_instances on the dedicated server, scan for entries whose ZNetView is
## Unity-destroyed OR whose view.GetZDO() returns null, and remove just the dict
## entry (the GameObject is left alone). These are exactly the entries that would
## NRE vanilla RemoveObjects, so we're only purging things vanilla can't handle.
## Triggered by mods that block WearNTear.RPC_Remove on the server while ZDOMan
## still reaps the ZDO via a separate path — e.g. TargetPortalProtection's
## Player.m_localPlayer-based permission check on a headless dedi when ZDO
## ownership has been moved to the server (FGN's Server-Side Simulation).
## 
## Every prune is logged at warning level (rate-limited to once per 5s) and the
## per-window count appears in the [ServerStatus] CDO segment. If you see this
## firing repeatedly on a healthy server, another mod is mismanaging ZNetScene
## state and should be investigated. Disable as a kill switch if it ever causes
## trouble (you'd then see the original NRE caught by the existing fallback).
# Setting type: Boolean
# Default value: true
Enable Instance Orphan Prune = true

## Fixes a Valheim 1.0 bug. When something jumps out of a player's area in one step
## (a portal, a teleport command), the server tests the position it is LEAVING, never tells
## that player, and they keep seeing the traveller frozen where they left until it next
## crosses a zone line. Re-runs vanilla's own check once the new position has landed.
## Stands down on its own when the game or another mod already fixes it, e.g.
## ValheimCommunityPatch's 'Fix Teleport Ghost Players' (deferred to while that is on).
## SERVER / LISTEN-HOST ONLY. No effect on a connecting client.
# Setting type: Boolean
# Default value: true
Fix Teleport Ghost Players = true

## Vanilla moves the sleep time skip forward by one physics step per rendered frame, so the
## skip meant to last 12 seconds takes longer the lower the server's frame rate. A busy server
## can keep everyone in bed for a minute or more. With this on, the skip follows real time and
## morning arrives after about 12 seconds however busy the server is.
## SERVER / LISTEN-HOST ONLY. No effect on a connecting client.
# Setting type: Boolean
# Default value: true
Fix Slow Sleep On Busy Servers = true

## Valheim disconnects a player whose keepalive goes unanswered for 30 seconds, and its keepalives
## wait behind everything already queued to send. A large transfer, such as a big base loading in,
## could time a player out while data was still arriving. With this on, keepalives go ahead of the
## queue. Works on whichever side has it; install on server and clients to cover both directions.
# Setting type: Boolean
# Default value: true
Keep Busy Connections Alive = true

## Every 2 seconds the server corrects each player's clock, and vanilla applies the correction at once.
## Waves are worked out from that clock, so every correction moves the water under a ship in a single
## physics step, and a boat with players aboard takes the jump as slamming into the water and loses hull
## health. The more a connection's timing wobbles, the bigger and more frequent the hits. With this on,
## waves follow a clock that eases each correction in over a few seconds, so the water never jumps.
## Corrections of 5 seconds or more (sleeping, reconnecting) still apply at once, as in vanilla.
## CLIENT-SIDE. A ship is damaged by the game of the player who owns it, normally someone aboard,
## so every player who sails needs this on. No effect on a dedicated server.
# Setting type: Boolean
# Default value: true
Fix Boat Damage From Server Time Sync = true

